{% extends 'base.html.twig' %}

{% block title %}Edit Offre{% endblock %}

{% block body %}
    <h1>Edit Offre</h1>

    {{ include('offre/form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('offre_index') }}">back to list</a>

    {{ include('offre/delete_form.html.twig') }}
{% endblock %}
